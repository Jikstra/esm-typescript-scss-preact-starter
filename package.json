{
  "name": "test-ts",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "http-server": "^0.12.1",
    "preact": "^10.4.0"
  },
  "devDependencies": {
    "@magic-works/ttypescript-browser-like-import-transformer": "^2.0.2",
    "snowpack": "^1.6.0",
    "ttypescript": "^1.5.10",
    "typescript": "^3.8.3"
  },
  "scripts": {
    "serve": "http-server ./dist",
    "clean": "rm -rf ./dist/",
    "build": "npm run build:static && npm run build:ts",
    "build:static": "mkdir -p ./dist && cp -r ./static/* ./dist",
    "build:ts": "npx ttsc --build tsconfig.json",
    "watch-ts": "npx ttsc --build tsconfig.json -w",
    "snowpack": "npx snowpack",
    "test": "echo \"Error: no test specified\" && exit 1",
    "first-start": "npx snowpack && npm run build-frontend && npm run dev-server"
  },
  "author": "",
  "license": "ISC"
}
